services:
  db:
    image: mysql:8.0
    container_name: docker1db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Ram@19
      MYSQL_DATABASE: trainee_management_db
    ports:
      - "3308:3306"
    volumes:
      - mysql_docker_data:/var/lib/mysql
 
volumes:
  mysql_docker_data: